# CTS500

The [CTS500](https://www.alibaba.com/product-detail/Mini-Climbing-Training-Scale-CTS500-Aluminum_1601637814595.html),
sold as the “Jlyscales CTS500” by
[Hunan Jinlian Cloud Information Technology Co., Ltd.](https://www.huaying-scales.com/), appears to be a China-made copy
of the Tindeq Progressor.

## Basic usage

```ts
import { CTS500 } from "@hangtime/grip-connect"

const device = new CTS500()
device.notify((data) => console.log(data.current))

await device.connect(
  async () => {
    console.log("Battery:", await device.battery())
    console.log("Model:", await device.model())
    console.log("Serial:", await device.serial())
    await device.stream(5000)
    device.disconnect()
  },
  (err) => console.error(err),
)
```

## Methods

CTS500 supports all [shared methods](/devices/#shared-methods) (connect, disconnect, isConnected, notify, active, read,
write, download). See [Device interface](/api/device-interface) for details.

### Device-specific

| Method                    | Returns                        | Description                               |
| ------------------------- | ------------------------------ | ----------------------------------------- |
| `battery()`               | `Promise<string \| undefined>` | Battery voltage.                          |
| `firmware()`              | `Promise<string \| undefined>` | Firmware version.                         |
| `firmwareUart()`          | `Promise<string \| undefined>` | Raw UART firmware payload as hex.         |
| `hardware()`              | `Promise<string \| undefined>` | Hardware version.                         |
| `manufacturer()`          | `Promise<string \| undefined>` | Manufacturer info.                        |
| `model()`                 | `Promise<string \| undefined>` | Model number.                             |
| `serial()`                | `Promise<string \| undefined>` | Serial number.                            |
| `software()`              | `Promise<string \| undefined>` | Software version.                         |
| `stream(duration?)`       | `Promise<void>`                | Start automatic weight uploads.           |
| `stop()`                  | `Promise<void>`                | Stop automatic weight uploads.            |
| `temperature()`           | `Promise<string \| undefined>` | Temperature in Celsius.                   |
| `weight()`                | `Promise<number \| undefined>` | Read the current weight in kilograms.     |
| `zero()`                  | `Promise<void>`                | Update the hardware zero point.           |
| `powerOnReset(enabled)`   | `Promise<void>`                | Toggle reset-to-zero on power-up.         |
| `peakMode(enabled?)`      | `Promise<void>`                | Toggle peak hold mode.                    |
| `setBaudRate(baudRate)`   | `Promise<void>`                | Configure the UART baud rate.             |
| `setSamplingRate(rateHz)` | `Promise<void>`                | Configure the A/D sampling rate in hertz. |

`firmware()` reads the Firmware Revision String of the Bluetooth module. `firmwareUart()` sends `GET_FIRMWARE_VERSION`
(`0xA4`) over the UART service. It returns the three raw MCU bytes as uppercase hex, for example `"20 3A 96"`. It does
not decode them. Browsers never get the serial number, because the Web Bluetooth blocklist hides `0x2A25`. On other
platforms, `serial()` returns the serial number when the module has the characteristic. Otherwise it returns
`undefined`.

## Command acknowledgments

Some CTS500 units do not answer a command with a 6-byte echo. They answer with a typed 7-byte frame:
`05 80 <opcode> 00 00 00 <checksum>`. For example, stop (`05 AB`) gets `05 80 AB 00 00 00 30`.

The tested unit has an MCU that answers `20 3A 96` to the `GET_FIRMWARE_VERSION` command (`0xA4`). Its Bluetooth module
is a MY-BT102 with software revision 2.1.3 and firmware revision 109a. `software()` and `firmware()` return these module
values. They do not identify the MCU.

The client accepts the typed answer and the 6-byte echo. On the tested unit, `stop()`, `zero()` and `peakMode()` get the
typed answer and resolve. `powerOnReset()` uses the same code path. Earlier releases waited for the echo only. As a
result, `stream(duration)` failed at the end, because its internal `stop()` timed out after the device had already
answered.

Tare (`05 A6`) gets no typed answer on the tested unit. The device answers with one weight frame, and `tare()` accepts
that frame. During a stream, this frame looks like any other weight frame, so it does not prove that the device applied
the tare.

Start (`05 AA`) sends no answer frame. It sends weight frames only. The client accepts the first weight frame as the
answer.

::: warning setBaudRate() can break the link

`setBaudRate()` changes the MCU UART speed. The Bluetooth module keeps its own speed. If the two speeds differ, the link
between them can stop working. Recovery can need a programmer. This was not tested on hardware. Leave the default
(`38400`) unless you can restore the device with a programmer.

:::

## Troubleshooting: connected but no data

A CTS500 can connect, resolve its services, and accept writes while the UART bridge between the MY-BT102 module and the
MCU passes no bytes. Then every command times out. Possible causes:

- The wiring between the module and the MCU.
- A module baud rate that differs from the MCU. The MCU default is `38400`.

A timeout error has the name `CTS500TimeoutError`. Its message starts with "Timed out waiting for CTS500 response" and
names the command, for example "to command 0xAB". If only one command times out, the firmware probably does not support
that command.

## Commands that the client does not wrap

| Command                                        | Opcode           | Reason                                                                                    |
| ---------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------- |
| Set range or capacity                          | `0x81`           | Changes the load-cell scaling. The protocol has no command to read the value back.        |
| Set division                                   | `0x82`           | Changes the load-cell scaling. The protocol has no command to read the value back.        |
| Set calibration reference weights              | `0x83`, `0x84`   | Overwrites calibration data. A repair needs known reference weights.                      |
| Run the first or second calibration            | `0xA1`, `0xA2`   | Overwrites calibration data. A repair needs known reference weights.                      |
| No-load calibration                            | `0xA7`           | Overwrites calibration data. A repair needs known reference weights.                      |
| Set shutdown time                              | `0xC3`           | Not wrapped yet.                                                                          |
| Set upper and lower temperature limits         | `0xC6`, `0xC7`   | Not wrapped yet.                                                                          |
| Set weight limits, alarm mode and alarm output | `0xD1` to `0xD4` | Alarm frames share the notify channel with weight frames. Their format is not documented. |

To send one of these commands, build the 6-byte frame with its additive checksum and pass it to `write()`.

See [Device interface](/api/device-interface) and [Guide](/guide) for connection and streaming.
